# Правила українського перекладу Underrail

Документ описує конвенції, витягнуті з уже відредагованих людиною сегментів (3346 із
63 505, станом на липень 2026). Це не побажання, а зафіксований стандарт: нові переклади
мають його виконувати, старі — приводяться до нього.

**Що вважається відредагованим.** Машинний переклад позначено `<target state="needs-translation">`,
відредагований людиною — `<target state="translated">`. Правила описують другу групу.
Перша перекладається наново і на неї покладатися не можна.

---

## 1. Розмітка рушія — найважливіше

Це не текст, а синтаксис. Помилка тут ламає діалог у грі, а не просто читається негарно.

| Елемент | Правило |
|---|---|
| `<end>` | Завершує репліку. **Завжди зберігати**, завжди в кінці. Ніколи не перекладати. |
| `::текст::` | Ремарка/нарація. Роздільники `::` зберігати, вміст перекладати як прозу. Кількість `::` у цілі має точно дорівнювати кількості в джерелі. |
| `::[Навичка]::` | Перевірка навички. Назву навички перекладаємо: `::[Persuade]::` → `::[Переконати]::`. |
| `{0}`, `{1}` | Підстановки рушія. Зберігати дослівно, не перенумеровувати. Пересувати в реченні — можна й треба. |
| `$(context.player.Name)` | Зберігати дослівно. |
| `$(#чол/жін)` | Гендерний вибір, див. §2. |

**Заборонено:** видаляти `<end>`; перекладати `<end>` словом; додавати `::` навколо
звичайної репліки; викидати ремарку `::He coughs.::` заради «гладкості».

## 2. Гендер гравця — `$(#…)`

Стать персонажа гравця невідома під час перекладу. Українська має рід у минулому часі,
прикметниках і багатьох іменниках — англійська здебільшого не має. Тому:

> **Плейсхолдерів в українському тексті буде більше, ніж в англійському. Це нормально і
> це обов'язково.**

Додавати `$(#…)` там, де форма залежить від статі гравця:

| Що | Приклад |
|---|---|
| дієслово минулого часу | `you helped` → `ти $(#допоміг/допомогла)` |
| дієслово 1-ї особи (репліка гравця) | `I've been there` → `я там $(#був/була)` |
| прикметник-присудок | `You're perceptive` → `Ти $(#проникливий/прониклива)` |
| іменник-діяч | `A fellow hunter` → `$(#Справжній/Справжня) $(#мисливець/мисливиця)` |
| звертання | `citizen` → `$(#громадянине/громадянко)` |
| займенник із джерела | `$(#him/her)` → `$(#нього/неї)` |

`sir/ma'am` → **`$(#пане/пані)`** (не «сер»).

Англійський плейсхолдер ніколи не лишається в цілі неперекладеним — гра покаже
«sir/ma'am» посеред української фрази.

## 3. Звертання: «ти» чи «ви»

1. **Нарація `::…::` — завжди «ви».** Виконується без винятків: `::He shifts his attention
   to you::` → `::Він переводить погляд на вас::`.
2. **Мова NPC — за характером персонажа.** Грубі, свої, вуличні (Big Bret, Аль Фабет,
   п'яниця на мосту) — «ти». Формальні, службові, обслуга (Бабар на Арені, кур'єри,
   сестри) — «ви».
3. **Репліки гравця дзеркалять співрозмовника.** Якщо NPC каже гравцеві «ти», гравець
   відповідає на «ти», і навпаки. У файлі `uk.xml` репліки NPC мають id `=>qN`, репліки
   гравця — `=>aN`; ця конвенція тримається без винятків.
4. **Наказовий спосіб успадковує вибір:** «ти»-персонажі — `Повертайся`, `перестань`;
   «ви»-персонажі — `Заходьте`, `Подумайте`.
5. **Підказки дій усередині `::…::` — інфінітив**, незалежно від регістру:
   `::Barter.::` → `::Торгувати.::`, `::Leave.::` → `::Піти.::`, `::Fight!::` → `::Битися!::`.

**Свідомі винятки — персонажі, що перемикають регістр за настроєм.** Такі переходи
зберігаємо, це задум, а не помилка:

- `cc_babar` — «ви» з відвідувачем Арени, але зривається на «ти», коли грубить, і
  повертається на «ви», щойно впізнає гравця.
- `cc_al_fabet` — «ви» як торговець (у джерелі він каже `$(#sir/ma'am)`), але «ти», коли
  роздратований або збуджений («Ти з потяга випав?»).

Базовий регістр персонажа визначається його спокійними репліками; репліки гравця
дзеркалять саме базовий, а не емоційні зриви.

## 4. Типографіка

| Знак | Використовуємо | Не використовуємо |
|---|---|---|
| Апостроф | `’` (U+2019) — `здоров’я`, `ім’я` | `'` (U+0027) |
| Лапки | `« »` | `" "`, `„ “` |
| Тире | `–` (en-dash, U+2013) з пробілами | `—` (em-dash), дефіс у ролі тире |
| Трикрапка | `…` (U+2026) | `...` |

Дефіс `-` лишається дефісом усередині слів (`псі-жуки`, `12,7-міліметрові`) і в
діапазонах (`{1}-{2}`, `16-40`). Тире ставимо тільки там, де з обох боків пробіли.

Латинські фрагменти зберігають свою типографіку: `Moe's`, `T.N.T`, `Shift + Left Click`
не чіпаємо.

## 5. Великі літери

**Загальне правило — українська sentence-case:** з великої лише перше слово, навіть якщо
англійська пише Title Case.

`Critical Hit Chance` → `Шанс критичного удару` · `Assault Rifle` → `Штурмова гвинтівка` ·
`Base Abilities` → `Базові здібності` · `Fortitude` → `Сила духу` (не «Сила Духу»).

**Виняток — складені топоніми пишемо Title Case, усі слова:**

`Core City` → `Центральне Місто` · `Deep Caverns` → `Глибокі Печери` ·
`Mushroom Cove` → `Грибна Бухта` · `Rail Crossing` → `Залізничний Переїзд` ·
`Black Sea` → `Чорне Море` · `Camp Hathor` → `Табір Хатор`.

> ⚠️ Це **свідоме відхилення від чинного правопису**, за яким родове слово мало б бути з
> малої («Грибна бухта», «Чорне море»). Обрано заради візуальної єдності назв локацій в
> інтерфейсі гри. **Не «виправляйте» назад** — це не помилка.

Капслок джерела зберігаємо: `DOMINATING` → `ДОМІНУЮЧА`.

## 6. Імена та транслітерація

| Категорія | Правило | Приклад |
|---|---|---|
| Особові імена | Транслітерувати, відмінювати | `Wyatt Pear` → `Ваятт Пір`, кличний: `Він мертвий, Брете.` |
| Назви істот | За глосарієм | `rathound` → `щурчак`, `burrower` → `копака` |
| Топоніми зі значенням | Перекладати | `Core City` → `Центральне Місто` |
| Топоніми без значення | Транслітерувати | `South Gate Station` → `Станція Саут-Гейт` |
| Корпорації, фракції-бренди | **Лишати латиницею** | `Underrail`, `Biocorp`, `Coretech`, `JKK`, `GMS`, `Praetorian Security` |
| Технічні акроніми | Лишати латиницею | `VSync`, `HUD`, `SCT`, `W2C`, `C-4` |
| Імена файлів, системний текст | Лишати дослівно | `Musical_Cipher_Keys.odc.rec` |

`Underrail` не відмінюється й не транслітерується навіть усередині української фрази:
`метросистеми Underrail`, `Південна Underrail`.

Латинський `h` передаємо через `Г` (`Harlan` → `Гарлан`, `Schopenhauer` → `Шопенгауер`).
Літера `Ґ` у проєкті не вживається. Виняток, закріплений глосарієм: `Halidorson` → `Халідорсон`.

## 7. Числа, одиниці, скорочення

- Десятковий роздільник — кома: `8.6mm` → `8,6`.
- Відсоток без пробілу: `{0}%`.
- Діапазони через дефіс без пробілів: `16-40`.
- Одиниці часу — одна літера без крапки: `h`→`г`, `m`→`хв`, `s`→`с`, `t`→`х`; пишуться
  впритул до підстановки: `{0}с`.
- Скорочення — з крапкою: `Еф. лікування`, `крит. удару`, `(дворучн.)`.
- Форми множини (1/кілька/багато) розрізняємо: `раз` / `рази` / `разів`.

## 8. Стиль інтерфейсу

- **Кнопки й команди — інфінітив:** `Delete` → `Видалити`, `Accept` → `Прийняти`,
  `End Turn` → `Закінчити хід`.
- **Вкладки й заголовки — іменники:** `Options` → `Налаштування`, `Credits` → `Творці`.
- **Описи дій предметів — інфінітив:** `Place a mine…` → `Розмістити міну…`.
- **Описи пасивних ефектів — 3-тя особа теперішнього:** `Increases your chance…` →
  `Збільшує ваш шанс…`.
- **В описах предметів і правил до гравця звертаємось на «ви».**
- Для вузьких елементів скорочуємо, а не переносимо: `Fullscreen mouse lock` →
  `Захопити мишу в повн. екр.`

## 9. Регістр і лайка

Грубе лишається грубим — не пом'якшуємо. Ідіоми локалізуємо, а не калькуємо.

`ugly as siphoner's ass` → `бридкий, як срака висмоктувача` ·
`for crying out loud` → `щоб його` · `What the hell!?` → `Що за чортівня!?` ·
`Suit yourself.` → `Як хочеш.` · `Huey, Dewey, and Louie` → `Крячик, Квачик і Кручик`.

Заїкання й дефекти мови відтворюємо на українському приголосному:
`Y-y-yes!` → `Т-т-так!`, `p-pipeworker` → `т-труболаз`.

Внутрішньоігровий сленг лишаємо як запозичення: `zoner` → `зонер`.

## 10. Канонічна термінологія

Повний перелік — у [глосарії проєкту](https://translate.flying-asparagus-factory.cc/projects/underrail/glossary/uk/)
у Weblate. Нижче — рішення щодо термінів, які досі перекладалися по-різному. У дужках —
кількість відредагованих сегментів із кожним варіантом.

| Англійська | Канон | Що зведено до нього |
|---|---|---|
| damage | **шкода** | `ушкодження`, `пошкодження` |
| points (здоров'я, псі) | **очки** | `пункти`, `бали` |
| stack | **накопичення** | `стек`; дієслово — «накопичується до N разів» |
| psi ability | **псі-здібність** | `псі здібність`, `психічна здібність`, `Псі-здатність` |
| psionic | **псіонічний** | `психічний` (але «психічний зрив» — не термін, лишається) |
| Blueprint | **Креслення** | `схема` |
| Oddity | **диковина** | `чудернацькі знахідки`, `дивні речі` |
| Constitution | **Статура** | `витривалість` |
| Temporal Manipulation | **Маніпулювання часом** | `Маніпуляція часом` |
| Cooldown | **відновлення** | — |
| Character Sheet | **Лист персонажа** | `Характеристики персонажа` |
| unarmed | **голіруч** | `голруч`, `голими руками` як назва категорії |
| Corroded (дебаф) | **Корозія** | неперекладений `Corroded` |
| Rig (локація) | **Платформа** | `Ріг` |
| Dodge / Evasion | **Вивертання** / **Ухилення** | це **різні** навички гри, плутати не можна |

**Типи шкоди — прикметники, увесь ряд:** Механічний, Тепловий, Крижаний, Електричний,
Кислотний, Енергетичний, Біологічний. У тексті так само: «крижана шкода», «теплова шкода».

**Рівні складності — прикметники жіночого роду** (узгоджені зі словом «складність»):
Початкова, Ветеранська, Експертна, Домінуюча, Налаштована. Окремий набір в `ui`
(Легка / Нормальна / Важка / ДОМІНУЮЧА) лишається як є.

**Скорочення зброї дзеркалять джерело:** `submachine gun` → «пістолет-кулемет», `SMG` → «ПК»;
`light machine gun` → «легкий кулемет», `LMG` → «ЛК». Повна форма перекладається повною,
скорочення — скороченням.

Термінологічні пари, які треба розводити за значенням, а не уніфікувати:

- `Stealth` — **Непомітність** як назва навички, **Скрадання** як назва режиму пересування.
- `схема` — лише електронна схема (`Психотронна схема`); креслення для майстрування — **Креслення**.
- `Foundry` — **Ливарня** як місто, але `Foundry Guard` — назва фракції, лишається латиницею,
  як і решта назв організацій.
- Слова, що позначають реальне пошкодження речей, а не ігрову шкоду, лишаються собою:
  «Файл пошкоджений», «сильно пошкоджені резервуари».

## 11. Перевірка

`tools/check-uk.ps1` перевіряє механічні інваріанти по всіх відредагованих сегментах:
збереження `<end>`, збіг кількості `::`, збіг наборів `{N}` та `$(context.…)`, відсутність
латиниці всередині `$(#…)`. Має давати нуль за всіма категоріями.

Скрипт не перевіряє стиль, термінологію й граматику — це залишається за редактором.
